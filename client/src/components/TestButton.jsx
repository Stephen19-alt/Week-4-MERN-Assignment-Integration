import { Button } from "@/components/ui/button";

const TestButton = () => {
  return (
    <div className="p-6">
      <Button variant="default">I’m a ShadCN Button</Button>
    </div>
  );
};

export default TestButton;
